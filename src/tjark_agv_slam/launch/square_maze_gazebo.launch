<launch>
  <!-- ============================================================ -->
  <!-- Square Maze Gazebo - 只启动 Gazebo + 机器人，用于验证新地图   -->
  <!--                                                              -->
  <!-- 不启动 gmapping、Cartographer、AMCL、move_base                -->
  <!-- 只提供 boxhouse_square_maze.world 的仿真环境                   -->
  <!--                                                              -->
  <!-- Usage:                                                       -->
  <!--   roslaunch tjark_agv_slam square_maze_gazebo.launch         -->
  <!-- ============================================================ -->

  <!-- 1. Load robot description -->
  <param name="robot_description"
         command="$(find xacro)/xacro $(find tjark_agv_slam)/urdf/tjark_agv_slam.xacro" />

  <!-- 2. Start Gazebo with square maze world -->
  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find tjark_agv_slam)/worlds/boxhouse_square_maze.world" />
    <arg name="paused" value="false" />
    <arg name="use_sim_time" value="true" />
    <arg name="gui" value="true" />
    <arg name="headless" value="false" />
    <arg name="debug" value="false" />
  </include>

  <!-- 3. Spawn robot at maze starting point (delay 5s for Gazebo ready) -->
  <node name="spawn_model" pkg="gazebo_ros" type="spawn_model"
        args="-urdf -model tjark_agv -param robot_description -x -2.8 -y -2.8 -z 0.4 -Y 0"
        output="screen"
        launch-prefix="bash -c 'sleep 5; $0 $@' " />

  <!-- 5. Robot state publisher -->
  <node name="robot_state_publisher" pkg="robot_state_publisher"
        type="robot_state_publisher" />

  <!-- Note: No SLAM, no AMCL, no move_base -->
  <!-- Use teleop in separate terminal: rosrun teleop_twist_keyboard teleop_twist_keyboard.py -->

</launch>
